### Graphics LIB
Graphics provides all graphical building blocks. Currently can create a wayland window in Linux and draw a box with OpenGL. Also supports MSDF font rendering. Will add blocks and support other platforms in the future.
